import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        margin: 10
    },
    name: {

    },
    title: {
        fontSize: 20
    }
})

export default styles;