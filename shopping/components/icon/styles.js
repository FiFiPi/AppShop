import { Platform, StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    shoppingcart: {
        marginRight: 20
    },
    menu: {
        marginLeft: 20
    },
    back: {
        marginLeft: 20,
    },
    filter: {
        marginRight: 20
    },
    delete: {
        marginRight: 20
    }
})

export default styles